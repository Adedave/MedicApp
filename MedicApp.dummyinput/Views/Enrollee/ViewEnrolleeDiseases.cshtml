@model List<DiseaseModel>
@{
    ViewData["Title"] = "ViewEnrolleeDiseases";
}

<h1>ViewEnrolleeDiseases</h1>

<h1>Here are the diseases attached to Enrollee of Id @ViewBag.EnrolleeId</h1>
<table class="table table-striped table-bordered table-hover" id="sample_3">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Disease Name
            </th>
            <th>
                Date Disease was Diagnosed
            </th>
            <th>
                Enrollee Diseases
            </th>
            <th>

            </th>
        </tr>
    </thead>

    <tbody>
        @{ foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.DiseaseName
                    </td>
                    <td>
                        @*@item.DateDiagnosed*@
                    </td>
                    <a asp-controller="Disease" asp-action="ViewEnrolleeOfDisease" asp-route-id="@item.Id">View List of Enrollees diagnosed with this Disease</a>
                    @*@{ foreach (var disease in item.Diseases)
                            {
                                <tr>
                                    <td>
                                        @disease.DiseaseName
                                    </td>
                                </tr>
                            }
                        }*@
                    <td>
                        assign-disease
                        @Html.ActionLink("Edit", "UpdateEnrollee", new { Id = item.Id })
                        @Html.ActionLink("Delete", "DeleteEnrollee", new { Id = item.Id })
                        @Html.ActionLink("Assign Disease", "AssignDisease", new { Id = item.Id })
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
